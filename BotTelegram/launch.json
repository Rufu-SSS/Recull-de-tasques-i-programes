{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Bot",
            "type": "coreclr",
            "request": "launch",
            "program": "${workspaceFolder}/bin/Debug/net8.0/TelegramBot.dll",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}