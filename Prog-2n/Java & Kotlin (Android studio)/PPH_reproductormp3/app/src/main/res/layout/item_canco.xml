<?xml version="1.0" encoding="utf-8"?>
<!-- Fila individual de la llista: portada + títol/artista + durada -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:background="?attr/selectableItemBackground">
    <!-- selectableItemBackground afegeix efecte ripple en fer clic -->

    <!-- Portada petita de la cançó: quadrat de 48dp -->
    <ImageView
        android:id="@+id/itemCover"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="centerCrop"
        android:clipToOutline="true"/>

    <!-- Títol i artista apilats verticalment al centre -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingEnd="8dp">

        <!-- Títol de la cançó: blanc, tallat amb "..." si és massa llarg -->
        <TextView
            android:id="@+id/itemTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:maxLines="1"
            android:ellipsize="end"/>

        <!-- Artista: gris clar, més petit que el títol -->
        <TextView
            android:id="@+id/itemArtist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#AAAAAA"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"/>
    </LinearLayout>

    <!-- Durada de la cançó en format MM:SS a la dreta de la fila -->
    <TextView
        android:id="@+id/itemDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#AAAAAA"
        android:textSize="12sp"/>

</LinearLayout>